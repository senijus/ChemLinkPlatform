# 工业监控系统 - 详细项目规划

## 项目概述

基于 i.MX6ULL ARM 平台的工业监控系统，集成 LVGL 触摸屏显示、RS485/Modbus 和 CAN 总线设备采集、MQTT 云端上报、SQLite 本地存储。本规划针对现有代码的 7 个问题逐一设计解决方案。

---

## 问题一：操作员无法切换为管理员

### 问题分析
当前 `ui_common.c` 状态栏的 "切换用户" 按钮在 `ROLE_OPERATOR` 状态下不会显示（逻辑只在 `ROLE_ADMIN` 时显示登出按钮）。操作员登录后没有任何入口切换到管理员。

### 解决方案
在状态栏始终显示一个"切换用户"按钮：
- **操作员已登录时**：点击"切换用户" → 弹出密码输入对话框 → 输入管理员密码 → 验证通过后切换为管理员
- **管理员已登录时**：点击"切换用户" → 直接登出（回到登录页）

### 涉及文件
| 文件 | 修改内容 |
|------|---------|
| `ui_common.c` | 状态栏按钮逻辑重构：操作员显示"切换用户"按钮，管理员显示"登出"按钮 |
| `ui_common.h` | 新增弹出对话框相关声明 |
| `auth.h` / `auth.c` | 无需修改，`AuthLogin()` 已支持 |

### 实现细节
```
状态栏按钮逻辑：
  ROLE_NONE   → 不显示按钮
  ROLE_OPERATOR → 显示 "切换用户" 按钮
    → 点击弹出 lv_msgbox + lv_textarea + lv_keyboard
    → 输入密码验证 → AuthLogin("admin")
    → 成功：刷新权限，显示 Settings 页面，按钮变为 "登出"
    → 失败：提示错误
  ROLE_ADMIN → 显示 "登出" 按钮
    → 点击执行 AuthLogout()，返回登录页
```

---

## 问题二：告警信息缺少具体原因

### 问题分析
当前 `alarm_mgr.c` 的 `AlarmRecord_t` 包含 type/value/threshold，但 `ui_alarm.c` 显示时缺少明确的中文原因描述。用户看到"温度告警"但不知道是"温度过高"还是"温度过低"，也不知道具体设备来源。

### 解决方案
增强告警记录的显示信息：
1. 每条告警记录显示完整的中文描述，格式：`"[来源] [参数]过高/过低: 当前值 XX > 阈值 YY [单位]"`
2. 告警类型增加中文描述映射
3. 状态栏告警指示器点击可跳转到告警页面

### 涉及文件
| 文件 | 修改内容 |
|------|---------|
| `alarm_mgr.h` | `AlarmRecord_t` 增加 `source` 字段完善（已有），增加告警描述格式化函数声明 |
| `alarm_mgr.c` | 新增 `AlarmMgrFormatDesc()` 函数，生成完整中文描述 |
| `ui_alarm.c` | 告警列表项显示格式优化，显示完整原因 |
| `ui_common.c` | 告警指示器点击事件 → 跳转告警页面 |

### 告警描述格式示例
```
[本地] 温度过高: 当前 85.2℃ > 阈值 80℃
[RS485-设备3] 电压过高: 当前 25.6V > 阈值 24V
[CAN-设备0x100] 转速过高: 当前 3200rpm > 阈值 3000rpm
[本地] 湿度过高: 当前 92.1% > 阈值 90%
```

### AlarmMgrCheck 改造
当前代码中告警检查只对本地传感器做了一次性判断，需要改为：
- 对每个 RS485 设备单独检查电压/电流
- 对每个 CAN 设备单独检查转速/流速
- 告警 source 字段格式化为 `"RS485-设备{id}"` / `"CAN-设备0x{id}"` / `"本地"`

---

## 问题三：邮箱消息队列无界堆积 → 有界队列 + 覆盖策略

### 问题分析
当前 `mailbox.c` 已经实现了有界队列（`max_len`），但存在两个问题：
1. 队列满时丢弃旧消息的策略只删除了队头一个，如果生产者速度持续快于消费者，队列仍然会在 max_len 附近反复触发
2. `cur_len` 的维护在某些边界条件下可能不精确

### 解决方案
确认当前实现已基本满足需求（`mailbox.c:252-269` 已实现满时丢弃最旧消息），优化以下细节：

### 涉及文件
| 文件 | 修改内容 |
|------|---------|
| `mailbox.c` | 优化队列满时的丢弃策略：丢弃到 `max_len * 80%` 再插入新消息，避免频繁触发 |
| `mailbox.h` | 无需修改，`max_len` 已定义 |

### 实现细节
```c
// 当前：满时只丢弃1个就插入
// 改为：满时丢弃到 80% 水位再插入，减少频繁 drop 的开销
if (ptmpPthreadNode->cur_len >= ptmpPthreadNode->max_len) {
    int target = ptmpPthreadNode->max_len * 4 / 5;  // 80% 水位
    while (ptmpPthreadNode->cur_len > target) {
        // 丢弃队头
    }
    LogWrite(LOG_LEVEL_WARN, "mailbox[%s] overflow, dropped %d msgs, now at %d/%d",
             name, dropped, cur_len, max_len);
}
```

---

## 问题四：分时发送策略 + 存储事务优化

### 问题分析
1. 当前 collect 线程已经实现了分时发送（lvgl 每次、storage 每3次、mqtt 每5次），但 RS485 和 CAN 线程没有分时，每次都发给所有模块
2. 存储模块已有事务机制（`StorageFlushBatch`），但批量大小和超时时间需要与分时策略配合

### 解决方案

#### 4.1 分时发送策略
```
采集频率：2秒/次（本地传感器），5秒/次（RS485轮询），事件驱动（CAN）

发送策略（以本地传感器 2 秒为基准）：
  → lvgl：    每次采集都发（2秒）     实时显示
  → storage： 每 3 次发一次（6秒）     批量写入
  → mqtt：    每 5 次发一次（10秒）    云端上报

RS485 采集线程（5秒轮询）：
  → lvgl：    每次都发（5秒）
  → storage： 每 2 次发一次（10秒）
  → mqtt：    每 2 次发一次（10秒）

CAN 监听线程（事件驱动）：
  → lvgl：    每帧都发
  → storage： 每 5 帧发一次
  → mqtt：    每 5 帧发一次
```

#### 4.2 存储事务优化
当前 `storage.c` 的 `StorageBatchInsert` 已有批量缓冲（10条）和超时刷新（30秒）。需要调整参数：

| 参数 | 当前值 | 建议值 | 理由 |
|------|--------|--------|------|
| `STORAGE_BATCH_SIZE` | 10 | 20 | 配合更大的写入量 |
| `STORAGE_BATCH_TIMEOUT_SEC` | 30 | 10 | 保证数据及时落盘 |

#### 4.3 涉及文件
| 文件 | 修改内容 |
|------|---------|
| `main.c` | RS485 线程增加 cycle 计数器实现分时；CAN 线程增加帧计数器实现分时 |
| `storage.c` | 调整 `STORAGE_BATCH_SIZE` 和 `STORAGE_BATCH_TIMEOUT_SEC` 参数 |
| `storage.h` | 无需修改 |

---

## 问题五：RS485 和 CAN 多设备数据区分 + 云端节点定义

### 问题分析
当前 RS485 和 CAN 数据已经通过 `Data_t` 中的数组结构传输，每个设备有独立的 `id`。但：
1. 云端 JSON 上报需要按 OneNET 的数组结构体格式组织
2. Dashboard 显示需要支持多设备数据展示
3. 存储需要支持按设备维度查询

### 解决方案

#### 5.1 RS485 数据协议定义
```
寄存器映射（每个从机，从寄存器 0 开始）：
  Reg 0: Volt 整数位
  Reg 1: Volt 小数位（0-99）
  Reg 2: Elec 整数位
  Reg 3: Elec 小数位（0-99）

计算公式：
  Volt = Reg[0] + Reg[1] / 100.0
  Elec = Reg[2] + Reg[3] / 100.0
```

#### 5.2 CAN 数据协议定义
```
帧结构（标准帧，8 字节数据）：
  Byte 0-2: Speed 整数位（大端，Byte0 为最高位）
  Byte 3:   Speed 小数位（0-99）
  Byte 4-6: Flow 整数位（大端，Byte4 为最高位）
  Byte 7:   Flow 小数位（0-99）

计算公式：
  Speed = (Byte[0]<<16 | Byte[1]<<8 | Byte[2]) + Byte[3] / 100.0
  Flow  = (Byte[4]<<16 | Byte[5]<<8 | Byte[6]) + Byte[7] / 100.0
```

#### 5.3 OneNET 云端 JSON 格式
```json
{
  "id": "123456",
  "params": {
    "Tem":   { "value": 25.6 },
    "Hum":   { "value": 60.3 },
    "Gas":   { "value": 45.2 },
    "Acc_xyz": { "value": 12.8 },
    "rs485_device": {
      "value": [
        { "id": 1, "Volt": 24.5, "Elec": 1.23 },
        { "id": 2, "Volt": 12.1, "Elec": 0.85 }
      ]
    },
    "can_device": {
      "value": [
        { "id": 256, "Speed": 1500.5, "Flow": 35.2 },
        { "id": 257, "Speed": 2200.0, "Flow": 48.7 }
      ]
    }
  }
}
```

#### 5.4 设备管理 UI（新增页面或设置页面内嵌）
在 Settings 页面或新增"设备管理"子页面：
- 显示当前已添加的 RS485 和 CAN 设备列表
- 提供"添加设备"按钮 → 弹出输入框输入设备 ID
- 每个设备旁边有"删除"按钮
- RS485 设备 ID 范围：1-247
- CAN 设备 ID 范围：0x000-0x7FF（11位标准帧）

#### 5.5 涉及文件
| 文件 | 修改内容 |
|------|---------|
| `mqtt.c` | `mqtt_send_all()` 中 RS485/CAN 数组按 OneNET 结构体数组格式构建 JSON |
| `ui_settings.c` | 新增设备管理区域（RS485 设备列表 + CAN 设备列表 + 添加/删除） |
| `main.c` | `Rs485AddDevice` / `CanAddDevice` 已实现，无需修改 |
| `storage.c` | 考虑新增 `rs485_device` 和 `can_device` 表，或在 `sensor_data` 表中增加 JSON 字段存储设备数据 |

### 5.6 数据库存储扩展（可选）
当前 `sensor_data` 表只存储本地传感器数据。RS485 和 CAN 设备数据可选择：
- **方案A**：在 `sensor_data` 表增加 `rs485_json` 和 `can_json` TEXT 字段，存储 JSON 格式的设备数组
- **方案B**：新增 `rs485_data` 和 `can_data` 表，每条记录对应一个设备的一次采样

建议采用**方案A**，简单且与 MQTT 上报格式一致。

---

## 问题六：断网续传

### 问题分析
当前 `pthread_mqtt` 线程已有断网续传逻辑：
1. 发送失败时进入 `offline_mode`
2. 重连成功后查询 `synced=0` 的记录重新发送
3. 发送成功后标记 `synced=1`

但存在以下问题：
1. `mqtt_reconnect()` 是阻塞的（内含 sleep），在 `pthread_mqtt` 的主循环中调用会阻塞 `MailBoxRecvMsg`
2. `StorageQueryUnsynced` 可能返回大量数据，一次性发送可能再次超时
3. 没有记录断网时间，无法判断哪些数据过期

### 解决方案

#### 6.1 非阻塞重连
将重连逻辑从主循环中分离，使用定时器或单独的重连机制：
```
mqtt 线程主循环：
  MailBoxRecvMsg（阻塞等待）
  ↓
  检查连接状态
  ├─ 已连接：
  │   ├─ 有未同步数据？→ 分批续传（每批 10 条）
  │   └─ 发送当前数据
  └─ 未连接：
      ├─ 标记数据 synced=0（由 storage 线程写入）
      └─ 尝试非阻塞重连（mqtt_reconnect 不 sleep，返回状态）
```

#### 6.2 分批续传
```c
#define RESUME_BATCH_SIZE 10

// 续传时每次只取 RESUME_BATCH_SIZE 条
DataEntry_t batch[RESUME_BATCH_SIZE];
int count = RESUME_BATCH_SIZE;
StorageQueryUnsynced(batch, &count);
for (int i = 0; i < count; i++) {
    mqtt_send_all(&batch[i].data);
}
StorageMarkSynced(batch, count);  // 只标记已发送的
```

#### 6.3 过期数据清理
超过 24 小时未同步的数据自动清理（避免数据库无限增长）：
```c
// 在 storage 线程中定期执行
StorageCleanOldData(1);  // 保留最近 1 天
```

#### 6.4 涉及文件
| 文件 | 修改内容 |
|------|---------|
| `main.c` | `pthread_mqtt` 重构：非阻塞重连 + 分批续传 + 过期清理 |
| `mqtt.c` | `mqtt_reconnect()` 改为非阻塞（去掉 sleep，返回连接状态） |
| `storage.h` | 新增 `StorageMarkSyncedBatch()` 和 `StorageCleanOldData()` 声明 |
| `storage.c` | 实现分批标记和过期清理 |

---

## 问题七：历史趋势图表优化（时间范围选取 + 降采样）

### 问题分析
当前 `ui_trend.c` 固定显示最近 100 个数据点，无法按时间段查询，也没有横坐标时间轴。

### 解决方案

#### 7.1 时间范围选取 UI
在趋势页面顶部增加时间范围选择控件：
- **快速选择**：按钮组 "1小时" / "6小时" / "24小时" / "自定义"
- **自定义范围**：两个日期时间选择器（开始时间、结束时间）
- **查询按钮**：触发从数据库加载数据

#### 7.2 数据库查询
```c
// storage.c 已有 StorageQueryData(start_time, end_time)
// 返回指定时间范围内的所有记录
```

#### 7.3 降采样算法
当查询结果超过 `MAX_CHART_POINTS`（建议 200）个点时，进行均匀降采样：

```c
// 均值降采样：将 N 个点分为 MAX_CHART_POINTS 组，每组取平均值
void downsample_avg(DataEntry_t *raw, int raw_count,
                    ChartPoint_t *out, int *out_count, int max_points) {
    if (raw_count <= max_points) {
        // 不需要降采样，直接复制
        for (int i = 0; i < raw_count; i++) {
            out[i].time = raw[i].timestamp;
            out[i].tem  = raw[i].data.Tem;
            out[i].hum  = raw[i].data.Hum;
            // ...
        }
        *out_count = raw_count;
        return;
    }

    double step = (double)raw_count / max_points;
    *out_count = 0;

    for (int i = 0; i < max_points; i++) {
        int start = (int)(i * step);
        int end   = (int)((i + 1) * step);
        if (end > raw_count) end = raw_count;

        double sum_tem = 0, sum_hum = 0, sum_gas = 0;
        int cnt = end - start;

        for (int j = start; j < end; j++) {
            sum_tem += raw[j].data.Tem;
            sum_hum += raw[j].data.Hum;
            sum_gas += raw[j].data.Gas;
        }

        out[*out_count].time = raw[start].timestamp;
        out[*out_count].tem  = sum_tem / cnt;
        out[*out_count].hum  = sum_hum / cnt;
        out[*out_count].gas  = sum_gas / cnt;
        (*out_count)++;
    }
}
```

#### 7.4 横坐标时间轴
使用 LVGL chart 的自定义刻度功能：
```c
// 设置横轴标签为时间格式
lv_chart_set_x_start_point(chart, ser, 0);

// 在 chart 下方覆盖一个 label 数组显示时间
// 或使用 lv_chart_set_axis_tick() 自定义刻度文本
```

由于 LVGL chart 的 x 轴刻度文本支持有限，建议：
- 在 chart 底部用 label 显示起始时间、中间时间、结束时间
- 或使用 `lv_chart_set_point_count()` 配合自定义绘制回调

#### 7.5 涉及文件
| 文件 | 修改内容 |
|------|---------|
| `ui_trend.c` | 重构：时间范围选择 UI、数据库查询、降采样、横坐标时间轴 |
| `ui_trend.h` | 新增时间范围相关结构体和函数声明 |
| `storage.c` | 优化 `StorageQueryData` 支持大数据量查询的性能（已有 timestamp 索引） |

---

## 涉及文件总览

| 文件 | 问题1 | 问题2 | 问题3 | 问题4 | 问题5 | 问题6 | 问题7 |
|------|-------|-------|-------|-------|-------|-------|-------|
| `mailbox.c` | | | ✓ | | | | |
| `mailbox.h` | | | | | | | |
| `main.c` | | | | ✓ | | ✓ | |
| `ui_common.c` | ✓ | ✓ | | | | | |
| `ui_common.h` | ✓ | | | | | | |
| `ui_login.c` | | | | | | | |
| `ui_dashboard.c` | | | | | | | |
| `ui_trend.c` | | | | | | | ✓ |
| `ui_trend.h` | | | | | | | ✓ |
| `ui_alarm.c` | | ✓ | | | | | |
| `ui_alarm.h` | | ✓ | | | | | |
| `ui_settings.c` | | | | | ✓ | | |
| `alarm_mgr.c` | | ✓ | | | | | |
| `alarm_mgr.h` | | ✓ | | | | | |
| `mqtt.c` | | | | | ✓ | ✓ | |
| `mqtt.h` | | | | | | ✓ | |
| `storage.c` | | | | ✓ | ✓ | ✓ | |
| `storage.h` | | | | | | ✓ | |
| `auth.c` | | | | | | | |
| `auth.h` | | | | | | | |

---

## 实施顺序建议

```
第一阶段（基础功能修复）：
  1. 问题3 - 邮箱队列优化（改动小，风险低）
  2. 问题1 - 操作员切换管理员（UI 层改动，独立）
  3. 问题2 - 告警详细信息（alarm_mgr + ui_alarm）

第二阶段（核心功能增强）：
  4. 问题4 - 分时发送 + 存储事务（main.c + storage.c）
  5. 问题6 - 断网续传优化（mqtt.c + main.c）
  6. 问题5 - 多设备数据区分 + 云端格式（mqtt.c + ui_settings.c）

第三阶段（体验优化）：
  7. 问题7 - 历史趋势图表重构（ui_trend.c，工作量最大）
```

---

## 补充说明

### 关于 LM75A 替代 Gas 传感器
- LM75A 读取的是温度值（-55℃ ~ +125℃）
- 需要映射到 1-100 范围作为 Gas 值：`Gas = (lm75a_temp + 55.0) / 175.0 * 99.0 + 1.0`
- 或者简单随机模拟 1-100

### 关于 LVGL 编译问题
当前 `ui_dashboard.c` 和 `ui_trend.c` 引用了 `data->Volt` 和 `data->Elec`，但 `Data_t` 中这些字段在 `rs485_dev[]` 数组内。需要修正为 `data->rs485_dev[0].Volt` 等（或在 Dashboard 中只显示第一个 RS485 设备的数据，或显示所有设备的平均值）。

### 关于 OneNET JSON 格式
RS485 和 CAN 设备数组在 OneNET thing-model 中需要按"结构体数组"格式上报，每个元素包含 `id`、`Volt`、`Elec`（或 `Speed`、`Flow`）字段。需要确认 OneNET 平台上已定义好对应的物模型属性。
