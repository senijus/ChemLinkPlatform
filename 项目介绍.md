# 多功能网关平台 - 项目介绍

## 1. 项目概述

本项目是一个基于 **ARM-Linux** 的多功能物联网网关平台，采用 **LVGL** 图形库构建嵌入式触摸屏人机交互界面（HMI），集成了多传感器数据采集、多总线设备通信、MQTT 云平台数据上传、SQLite 本地存储、告警管理等功能。系统运行于 i.MX6ULL 系列 ARM 处理器（正点原子/阿尔法开发板），通过 800x480 触摸屏提供直观的仪表盘式交互体验。

## 2. 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    用户界面层 (LVGL UI)                   │
│  ┌──────┐ ┌──────────┐ ┌──────┐ ┌──────┐ ┌──────────┐  │
│  │ 登录  │ │  仪表盘   │ │ 趋势 │ │ 告警 │ │   设置   │  │
│  └──────┘ └──────────┘ └──────┘ └──────┘ └──────────┘  │
├─────────────────────────────────────────────────────────┤
│                    核心业务层 (Core)                      │
│  ┌──────────┐ ┌────────┐ ┌───────┐ ┌─────┐ ┌────────┐  │
│  │ 告警管理  │ │ 日志系统│ │ 存储  │ │ MQTT│ │ 鉴权   │  │
│  └──────────┘ └────────┘ └───────┘ └─────┘ └────────┘  │
├─────────────────────────────────────────────────────────┤
│               硬件抽象层 (HAL) / 邮箱通信                 │
│  ┌──────────────────────┐  ┌─────────────────────────┐  │
│  │     MailBox 消息队列   │  │     Sensor HAL          │  │
│  └──────────────────────┘  └─────────────────────────┘  │
├─────────────────────────────────────────────────────────┤
│                    驱动层 (Drivers)                       │
│  ┌──────┐ ┌───────┐ ┌──────┐ ┌─────┐ ┌──────┐         │
│  │ DHT11│ │ADXL345│ │LM75A │ │ LED │ │ BEEP │         │
│  └──────┘ └───────┘ └──────┘ └─────┘ └──────┘         │
│  ┌──────────┐  ┌──────────┐                             │
│  │ RS485/Modbus│ │ CAN Bus  │                           │
│  └──────────┘  └──────────┘                             │
├─────────────────────────────────────────────────────────┤
│              Linux Kernel / 设备驱动模块                   │
└─────────────────────────────────────────────────────────┘
```

## 3. 技术栈

| 类别 | 技术 |
|------|------|
| **硬件平台** | i.MX6ULL ARM Cortex-A7 (正点原子开发板) |
| **操作系统** | Linux (内核 4.1.15) |
| **编译工具链** | arm-linux-gnueabihf-gcc |
| **GUI 框架** | LVGL v8.x |
| **数据库** | SQLite3 |
| **消息队列** | MQTT (OneNET 云平台, Paho MQTT C Client) |
| **工业总线** | RS485 (Modbus RTU) / CAN Bus |
| **加密** | OpenSSL |
| **线程模型** | POSIX Threads (pthreads) |
| **构建系统** | Makefile |

## 4. 目录结构

```
project_fin/
├── Makefile                    # 主构建文件
├── config/
│   ├── lv_conf.h               # LVGL 配置
│   └── lv_drv_conf.h           # LVGL 驱动配置
├── include/                    # 项目头文件
│   ├── alarm_mgr.h             # 告警管理
│   ├── auth.h                  # 用户鉴权
│   ├── dev_mgr.h               # 设备管理 (RS485/CAN)
│   ├── log.h                   # 日志系统
│   ├── lvgl_ui.h               # LVGL UI 主接口
│   ├── mailbox.h               # 线程间消息队列
│   ├── mqtt.h                  # MQTT 通信
│   ├── sensor_hal.h            # 传感器硬件抽象
│   ├── storage.h               # SQLite 持久化存储
│   ├── ui_alarm.h              # 告警页面
│   ├── ui_common.h             # UI 公共组件
│   ├── ui_dashboard.h          # 仪表盘页面
│   ├── ui_login.h              # 登录页面
│   ├── ui_settings.h           # 设置页面
│   └── ui_trend.h              # 趋势图表页面
├── src/
│   ├── core/                   # 核心业务逻辑
│   │   ├── main.c              # 主程序入口 & 多线程调度
│   │   ├── alarm_mgr.c         # 告警检测与管理
│   │   ├── auth.c              # 用户登录/鉴权
│   │   ├── log.c               # 日志记录
│   │   ├── mailbox.c           # 线程间邮箱通信
│   │   ├── mqtt.c              # MQTT 客户端
│   │   └── storage.c           # SQLite 数据存储
│   ├── hal/                    # 硬件抽象层
│   │   └── sensor_hal.c        # 传感器统一读取接口
│   └── ui/                     # 界面层
│       ├── lvgl_ui.c           # LVGL 初始化与主循环
│       ├── ui_common.c         # 状态栏/导航栏/页面切换
│       ├── ui_login.c          # 登录界面
│       ├── ui_dashboard.c      # 实时仪表盘
│       ├── ui_trend.c          # 历史趋势图表
│       ├── ui_alarm.c          # 告警记录列表
│       └── ui_settings.c       # 系统设置
├── drivers/                    # Linux 内核驱动模块
│   ├── dht11/                  # 温湿度传感器驱动
│   ├── adxl345_spi/            # 三轴加速度传感器驱动 (SPI)
│   ├── lm75a_i2c/              # 温度传感器驱动 (I2C)
│   ├── led_platform/           # LED 控制驱动
│   └── beep_platform/          # 蜂鸣器控制驱动
├── lvgl/                       # LVGL 图形库源码
├── lv_drivers/                 # LVGL 显示/输入驱动
└── lib/                        # 第三方库
    ├── paholib/                # Paho MQTT 库
    ├── openssllib/             # OpenSSL 库
    ├── sqlitelib/              # SQLite 库
    └── libmodbus/              # Modbus 协议库
```

## 5. 核心功能模块

### 5.1 多线程架构

系统采用 **邮箱（MailBox）消息队列** 模式实现多线程协作，各线程通过命名邮箱收发结构化数据：

| 线程 | 功能 | 采集周期 |
|------|------|---------|
| **collect** | 本地传感器采集 + RS485/CAN 数据聚合 | 2 秒 |
| **storage** | 数据持久化写入 SQLite | 6 秒 (批量) |
| **mqtt** | 云端数据上传 (OneNET) | 10 秒 |
| **lvgl** | GUI 界面刷新 | 2 秒 (实时) |
| **RS485** | Modbus RTU 从站轮询 | 5 秒 |
| **CAN** | CAN 总线数据监听 | 实时 |

### 5.2 传感器数据采集

通过硬件抽象层（HAL）统一管理多种传感器：

| 传感器 | 接口 | 采集数据 | 说明 |
|--------|------|---------|------|
| **DHT11** | GPIO | 温度(°C)、湿度(%RH) | 温湿度传感器 |
| **ADXL345** | SPI | 三轴加速度(g) | 振动/倾斜检测 |
| **LM75A** | I2C | 气体温度(°C) | 温度传感器，复用为气体检测指标 |

### 5.3 工业总线通信

- **RS485 (Modbus RTU)**：通过 `/dev/ttymxc2` 串口连接 Modbus 从站设备，读取电压(V)和电流(A)寄存器，支持动态添加/删除从站设备（最多 50 台）。
- **CAN Bus**：通过 `can0` 网络接口接收 CAN 帧，解析速度和流量数据，支持动态注册设备 ID（最多 50 个）。

### 5.4 MQTT 云平台通信

- 对接 **OneNET 物联网平台**（中国移动），采用 MQTT 协议上传传感器数据。
- 支持 **断网续传**：网络中断期间数据暂存本地 SQLite，恢复连接后自动分批补传。
- 指数退避重连策略（2s → 4s → 8s → ... → 60s）。

### 5.5 数据持久化 (SQLite)

- 传感器数据、告警记录、告警阈值、用户凭据均存储于 SQLite 数据库 (`info.db`)。
- 批量写入（事务优化）+ 定期 WAL checkpoint。
- 自动清理过期数据（默认保留 1 天）。
- 用户密码使用 OpenSSL 加密存储。

### 5.6 告警管理

- 支持 **16 种告警类型**：温度、湿度、气体、加速度、RS485 电压/电流、CAN 速度/流量，每种均有上限和下限告警。
- 告警触发时自动联动 **LED 指示灯** 和 **蜂鸣器**。
- 告警阈值可由用户在 UI 中自定义配置，持久化保存。
- 最多保存 100 条告警记录，支持确认和清空操作。

### 5.7 用户鉴权

- 双角色权限体系：**操作员 (Operator)** 和 **管理员 (Admin)**。
- 登录后根据角色控制 UI 功能可见性。
- 支持修改密码（加密存储于 SQLite）。

## 6. 用户界面 (LVGL)

界面采用 800×480 分辨率，包含以下页面：

### 6.1 全局组件
- **状态栏**：显示当前时间、用户角色、告警状态指示。
- **导航栏**：底部页面切换按钮（仪表盘/趋势/告警/设置），根据角色动态显隐。

### 6.2 功能页面

| 页面 | 功能描述 |
|------|---------|
| **登录页** | 密码输入登录，区分操作员和管理员 |
| **仪表盘** | 实时显示所有传感器数据卡片：温度、湿度、气体、加速度，以及 RS485/CAN 设备列表 |
| **趋势页** | 历史数据折线图，支持日期选择器查询 SQLite 历史记录 |
| **告警页** | 告警记录列表，显示时间、类型、数值、阈值，支持确认和清空 |
| **设置页** | 告警阈值配置、RS485/CAN 设备管理（增删）、LED/BEEP 手动控制、密码修改、数据清理 |

## 7. 内核驱动模块

所有驱动均以 **Linux 内核模块 (.ko)** 形式实现，采用设备树（Device Tree）匹配硬件：

| 驱动 | 设备节点 | 说明 |
|------|---------|------|
| DHT11 | `/dev/dht11` | GPIO 温湿度传感器 |
| ADXL345 | `/dev/adxl345` | SPI 三轴加速度传感器 |
| LM75A | `/dev/lm75a` | I2C 温度传感器 |
| LED | `/dev/led` | GPIO LED 控制 |
| BEEP | `/dev/beep` | GPIO 蜂鸣器控制 |

每个驱动模块包含：
- `xxx_drv/` — 内核驱动实现（字符设备 + miscdevice 框架）
- `xxx_app/` — 用户态测试应用
- `Makefile` — 内核模块编译

## 8. 构建与部署

```bash
# 编译应用程序（交叉编译）
make

# 编译内核驱动模块
make drivers

# 清理
make clean

# 编译产物自动拷贝至 NFS 根文件系统
# → ~/nfs/rootfs/mqtt_test/mqtt
```

**运行依赖**：
- 目标板 Linux 内核源码路径通过 `KERNDIR` 变量指定
- NFS 挂载用于开发阶段的文件传输
- 需要加载所有 `.ko` 驱动模块后运行可执行文件

## 9. 数据流示意

```
[DHT11] [ADXL345] [LM75A]     [RS485 从站]      [CAN 设备]
    │       │       │              │                  │
    └───────┴───────┘              │                  │
            │                      │                  │
      ┌─────▼─────┐         ┌─────▼─────┐      ┌────▼────┐
      │ Sensor HAL │         │ RS485 线程 │      │ CAN 线程 │
      └─────┬─────┘         └─────┬─────┘      └────┬────┘
            │                      │                  │
            └──────────┬───────────┴──────────────────┘
                       │
                 ┌─────▼─────┐
                 │ collect 线程│
                 │ (邮箱分发)  │
                 └──┬───┬───┬┘
                    │   │   │
         ┌──────────┘   │   └──────────┐
         ▼              ▼              ▼
   ┌──────────┐  ┌──────────┐  ┌──────────┐
   │ LVGL 线程│  │Storage线程│  │ MQTT 线程│
   │ (UI 刷新) │  │(SQLite)  │  │(OneNET)  │
   └──────────┘  └──────────┘  └──────────┘
```
